# CF-005: Doctor checkMemoryBackend reads env var + config.yaml instead of config.json
# GitHub: #1194
# Absorbed into CF-003 — kept for GitHub issue reference.
# CF-003a now writes the config.json reader directly; CF-005 ops are no-ops.
