# claude-flow-patch environment variables
# Copy to .env and fill in values (never commit .env)

# npm publish token (required for npm/publish.sh)
# Create at https://www.npmjs.com/settings/<user>/tokens — Classic, Publish scope
NPM_TOKEN=
